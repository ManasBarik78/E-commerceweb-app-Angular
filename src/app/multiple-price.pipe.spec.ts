import { MultiplePricePipe } from './multiple-price.pipe';

describe('MultiplePricePipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplePricePipe();
    expect(pipe).toBeTruthy();
  });
});
